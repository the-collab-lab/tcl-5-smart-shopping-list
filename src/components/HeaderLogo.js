import React from 'react';
import '../App.css';

const Header = () => {
    return (
        <header className="Header">
            <h1>Smart Shopping List</h1>
        </header>
    );
};

export default Header;
